<Window x:Class="WPF_MVVM.View.Main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_MVVM.View"
        mc:Ignorable="d"
        Title="Main" Height="450" Width="800">
    <Grid Background="#F5F5F5">
        <Grid Background="White" Grid.Row="1" Margin="25,40,25,15">
            <TextBox
            Name="FilterTextBox"
            Height="30"
            Width="200"
            Margin="25"
            HorizontalAlignment="Left" />

            <Button
            Height="30"
            Command="{Binding ShowWindowsCommand}"
                Content="Add User"
            Width="100"
            VerticalContentAlignment="Center"
            Padding="5"
            Margin="625,25,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top" />

            <Button
Height="30"
Command="{Binding ShowNewWindowsCommand }"
    Content="Show New Window"
Width="100"
VerticalContentAlignment="Center"
Padding="5"
Margin="500,25,0,0"
HorizontalAlignment="Left"
VerticalAlignment="Top" />
            <ListView
            Name="UserList"
            ItemsSource="{Binding Users}"
            BorderBrush="#cdcdcd"
            Margin="25,70,25,25"
            Padding="0" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" Width="120"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Email}" Header="Email" Width="120"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <TextBox Text="{Binding Name}" HorizontalAlignment="Left" Margin="27,33,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
            <Button
                Height="30"
                Command="{Binding SearchPlayerCommand }"
                Content="Search"
                Width="100"
                VerticalContentAlignment="Center"
                Padding="5"
                Margin="175,22,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top" />
        </Grid>
    </Grid>
</Window>
